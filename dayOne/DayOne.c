#include<stdio.h>
#include<conio.h>
void main() {
    int a,b,sum;
    printf("enter the a valude: ");
    scanf("%d",&a);
    printf("enter the b valude: ");
    scanf("%d",&b);
    sum = a + b;
    printf("%d",sum);
    getch();
}